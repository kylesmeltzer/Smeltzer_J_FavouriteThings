const config = {
    user: "root",
    pword: "root", //windows leave blank
    host: "localhost",
    ports: "8889", //windows users this will be 3306
    database: "marvelcharacters"
}

module.exports = config;